\begin{frame}[fragile,t]{Primeiro circuito: porta AND}
\begin{lstlisting}
library ieee;
use ieee.std_logic_1164.all;

entity porta_and is
  port (a, b : in  std_logic;
        y    : out std_logic);
end entity;

architecture rtl of porta_and is
begin
  y <= a and b;
end architecture;
\end{lstlisting}
\end{frame}
