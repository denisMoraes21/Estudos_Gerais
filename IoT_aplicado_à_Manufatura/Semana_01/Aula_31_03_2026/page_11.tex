\begin{frame}[fragile]{Revisão: Linguagem C}
    \begin{itemize}
        \item Laços de controle
    \end{itemize}

\begin{lstlisting}[language=C,basicstyle=\ttfamily\scriptsize]
#include <stdio.h>

int test_function(void) {
    for (int i = 0; i < 10; i++) {
        printf("Numero: %d \n", i);
    }
    
    int c = 0;
    while (c == 10) {
        printf("Contador: %d \n", c);
        c++; // c = c + 1;
    }
    return 0;
}
\end{lstlisting}

Cuidado com Loops infinitos!!!
\end{frame}