\begin{frame}[fragile]{Revisão: Linguagem C}
    \begin{itemize}
        \item Estruturas de seleção
    \end{itemize}

\begin{lstlisting}[language=C,basicstyle=\ttfamily\scriptsize]
#include <stdio.h>

int test_function(int number_1, int number_2) {
    int soma = number_1 + number_2;

    if (soma > 5) {
        printf("Soma maior que 5\n");
    } 
    else {
        printf("Soma insuficiente\n");
    }

    return soma;
}
\end{lstlisting}

\end{frame}