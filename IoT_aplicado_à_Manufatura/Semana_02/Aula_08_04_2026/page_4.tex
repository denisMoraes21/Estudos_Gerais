\begin{frame}[fragile]{Envio HTTP com autenticação}
\begin{lstlisting}[language=C, basicstyle=\tiny\ttfamily]
#include <WiFi.h>
#include <HTTPClient.h>

const char* serverURL = "http://seu-servidor.com/api";

void setup() {
    Serial.begin(115200);
}
\end{lstlisting}
\end{frame}
    
    